var colorans;
var colorseasy;
var colorshard

function start() {
    //document.getElementById
    //use innerhtml and create another div messagedisplay
}

function easy(){
    for (let i = 0; i<3; i++){
        const r = Math.floor(Math.random()*256);
        const g = Math.floor(Math.random()*256);
        const b = Math.floor(Math.random()*256);
        const color = [r,g,b];
    }
}

function hard(){
    for (let i = 0; i<6; i++){
        const r = Math.floor(Math.random()*256);
        const g = Math.floor(Math.random()*256);
        const b = Math.floor(Math.random()*256);
        const color = [r,g,b];
    }
}

function check(){

}